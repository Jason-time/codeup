#include <stdio.h>

int main(void) {
	
	int w,h,b,sum,i;
	long long int to;
	double re;
	
	scanf("%d %d %d", &w,&h,&b);  
	sum =w;
	for(i=0; i< b-1; i++){
		sum +=h;
		
		 
	}
		printf("%d ",sum);
	  
	return 0;
}
