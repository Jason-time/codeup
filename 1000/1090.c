#include <stdio.h>

int main(void) {
	
	int w,h,b,sum,i;
	long long int to;
	double re;
	
	scanf("%d %d %d", &w,&h,&b);  
	to =w;
	for(i=0; i< b-1; i++){
		to *=h;
		
		 
	}
		printf("%lld ",to);
	  
	return 0;
}
